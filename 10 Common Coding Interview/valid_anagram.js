// Given two strings s1 and s2, check if they're anagrams.
// Two strings are anagrams of they're made of the same characters with the same frequencies

//* Example:
// s1 = 'danger'
// s2 = 'garden'
// output: true

// Two strings are anagrams of they're made of the same characters
// with the same frequencies.

// Hash Table
